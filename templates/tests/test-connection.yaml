apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argocd-training.fullname" . }}-test-connection"
  labels:
    {{- include "argocd-training.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argocd-training.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
