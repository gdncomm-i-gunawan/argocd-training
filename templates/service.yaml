apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-training.fullname" . }}
  labels:
    {{- include "argocd-training.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-training.selectorLabels" . | nindent 4 }}
